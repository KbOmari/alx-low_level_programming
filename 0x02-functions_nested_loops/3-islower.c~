#include "main.h"
/**
 *_islower - entry point
 *
 *Description: Program that checks for lowercase character
 *
 *Return: (results)
*/

int _islower(int c)
{
	int x;

	if (x >= 097 && x <= 122)
		return (1);
	else
		return (0);

	return (results);
}

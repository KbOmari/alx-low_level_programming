#include <stdio.h>
#include <ctype.h>
/**
 *main - Entry point
 *
 *Return: Always (0) Success
 *
 */

int main(void)
{
	char c;

	for (c = '1'; c <= '9'; c++)

	putchar(c);
	putchar('\n');
	return (0);
}
